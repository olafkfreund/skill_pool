/result
/result-*
/target/
/node_modules/
/.direnv/
*.log
.DS_Store

# Phase 0 — local library cache; tracked separately by user installs
/.local-library/

# sqlx offline metadata — regenerated by `cargo sqlx prepare` in CI once schema stabilises
# (Phase 1 uses runtime-checked queries; we'll commit .sqlx/ when we switch to query!)
/.sqlx-skip/
