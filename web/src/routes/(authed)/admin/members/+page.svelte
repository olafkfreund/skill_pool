<header class="mb-8">
  <h1 class="text-2xl font-semibold">Members</h1>
  <p class="mt-1 text-sm text-[var(--sp-muted-fg)]">
    Lists tenant_users with role assignment. SCIM auto-provisioning lands in #8.
  </p>
</header>

<div
  class="rounded-[var(--sp-radius)] border border-dashed border-[var(--sp-border)] p-12 text-center text-sm text-[var(--sp-muted-fg)]"
>
  Coming in the next iteration of #4 — needs the <code>users</code> + <code>tenant_users</code>
  list endpoint on the server.
</div>
