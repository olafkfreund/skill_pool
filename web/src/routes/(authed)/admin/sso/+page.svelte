<header class="mb-8">
  <h1 class="text-2xl font-semibold">SSO</h1>
  <p class="mt-1 text-sm text-[var(--sp-muted-fg)]">
    SAML metadata upload, OIDC client config, SCIM endpoint + token. Wires up in #8.
  </p>
</header>

<div
  class="rounded-[var(--sp-radius)] border border-dashed border-[var(--sp-border)] p-12 text-center text-sm text-[var(--sp-muted-fg)]"
>
  Until then, sign in with an API token from <code>skill-pool-server admin token-create</code>.
</div>
